import { DetailBlock, DetailShell } from '../../components/detail-shell';
import { LiveEconomyTable } from '../../components/live-economy-table';
import { economyIndicators, sourceById, type Source } from '../../lib/data/research';
export default function EconomyPage() { const indicatorSources=Object.values(economyIndicators).map(item=>sourceById(item.sourceId)).filter((source):source is Source=>Boolean(source)); return <DetailShell eyebrow="Economy" title="The live economic record" summary="Four consistently defined calendar-year series refresh from the official World Bank API on each visit, with a verified local fallback." sources={indicatorSources}><DetailBlock title="Latest available is not instantaneous" text="World Development Indicators are revised annual series. “Live” means this page requests the newest official release directly; it does not imply a second-by-second measure of the economy."/><LiveEconomyTable/><DetailBlock label="METHODOLOGY NOTE" title="No causal attribution" text="A line rising or falling during a term is not proof that the government caused that movement. Annual values overlap government transitions, global shocks, state-level action and policy lags."/></DetailShell>; }
