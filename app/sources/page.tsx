import { DetailBlock, DetailShell } from '../../components/detail-shell';
import { sources } from '../../lib/data/research';
import { dossierSources } from '../../lib/data/dossiers';
export default function SourcesPage() { const allSources=[...sources,...dossierSources]; return <DetailShell eyebrow="Sources" title="The evidence library" summary={`${allSources.length} registered primary, official, institutional and party-document sources support this research edition.`} sources={allSources}><DetailBlock title="Source hierarchy" text="Primary law, gazette, budget, ministry and regulator records are preferred. Institutional comparison data is used where it provides a consistent definition across both periods. Party documents are used only to establish what a party committed to."/><DetailBlock title="Deep-dossier standard" text="Dossiers start with operative legal text, treaty records, judgments, regulator material or official implementation documents. Commentary identifies mechanisms and trade-offs; it does not relabel public paperwork as secret intelligence."/><DetailBlock title="Link and revision policy" text="Source metadata is retained even when an official portal changes its URL. Records should be re-reviewed when underlying data is revised, rules commence, courts alter legal interpretation or new primary documents become available."/></DetailShell>; }
