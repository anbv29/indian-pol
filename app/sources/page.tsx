import { DetailBlock, DetailShell } from '../../components/detail-shell';
import { sources } from '../../lib/data/research';
export default function SourcesPage() { return <DetailShell eyebrow="Sources" title="The evidence library" summary={`${sources.length} registered primary, official, institutional and party-document sources support this research edition.`} sources={sources}><DetailBlock title="Source hierarchy" text="Primary law, gazette, budget, ministry and regulator records are preferred. Institutional comparison data is used where it provides a consistent definition across both periods. Party documents are used only to establish what a party committed to."/><DetailBlock title="Link and revision policy" text="Source metadata is retained even when an official portal changes its URL. Records should be re-reviewed when underlying data is revised, rules commence, courts alter legal interpretation or new primary documents become available."/></DetailShell>; }
