import { ArrowLeft, ArrowUpRight, ShieldCheck } from 'lucide-react';

export function DetailShell({ eyebrow, title, summary, children }: { eyebrow: string; title: string; summary: string; children?: React.ReactNode }) {
  return <main className="detail-page"><div className="detail-glow" /><header><a href="/"><span>INDIA</span><b>THE RECORD</b></a><a href="/"><ArrowLeft size={14} /> Back to record</a></header><article><p className="detail-eyebrow">DEMO / SAMPLE DATA · {eyebrow}</p><h1>{title}</h1><p className="detail-lead">{summary}</p><div className="detail-meta"><span>Category <b>{eyebrow}</b></span><span>Evidence <b>Sample only</b></span><span>Last reviewed <b>Prototype</b></span></div>{children ?? <><DetailBlock title="What happened?" text="This section will present a concise factual summary based on the underlying source record." /><DetailBlock title="What changed?" text="Documented legal or policy changes will appear here, separate from official objectives and assessments." /><DetailBlock title="Documented outcome" text="Only measurable, source-backed outcomes will be included. This prototype intentionally contains no historical conclusion." /></>}<section className="detail-sources"><div><ShieldCheck size={18} /><span>SOURCE TRAIL</span></div><h2>Every claim should lead<br />back to evidence.</h2>{['Primary official document','Institutional dataset','Parliamentary record'].map((item, index) => <a href="/sources" key={item}><span>0{index + 1}</span><b>{item}</b><small>Source placeholder</small><ArrowUpRight size={14} /></a>)}</section></article></main>;
}

export function DetailBlock({ title, text }: { title: string; text: string }) { return <section className="detail-block"><span>FACTUAL LAYER</span><h2>{title}</h2><p>{text}</p></section>; }
